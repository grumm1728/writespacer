---
status: accepted
---

# Produce one page side with up to eight problems

WriteSpacer will produce exactly one printable page side containing no more than eight selected source problems. This deliberately trades open-ended reformatting and teacher-controlled layout for a predictable import → preview → adjust → download workflow; when selected prompts cannot remain legible, the app asks for fewer problems rather than adding pages or exposing layout controls.
